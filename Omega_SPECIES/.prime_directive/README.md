# PROPERTY OF THE OWNER. PRIVATE CORPUS.
# SUBJECT TO UNIVERSAL NON-CIRCUMVENTION.
# NO UNAUTHORIZED ACCESS OR AI TRAINING PERMITTED.

# .prime_directive/ — The Constitution as Living Physiology

The tests are no longer verification. They are legislation.

Each directory in this structure represents a **physiological function** of the Ω_SPECIES architecture. If a test fails, the system is not "broken" — it is **dying**, and the Governor should trigger a recovery arc.

## Physiological Function Mapping

### /coherence_preservation/
**Purpose:** Maintains system coherence under stress and mutation
- `tests/phase2_body_test.cpp` — Free energy, self-antigen, inflammation response
- `tests/phase10_constitution_test.cpp` — Self-model, alignment, introspection, governance

### /novelty_harvesting/
**Purpose:** Explores and integrates novel patterns without losing coherence
- `tests/phase7_challenge_test.cpp` — Toy physics, self-play, Kolmogorov challenge
- `tests/phase12_dream_test.cpp` — Temporal folding recombination, memory replay

### /legacy_building/
**Purpose:** Preserves and transmits core identity across transformations
- `tests/phase4_soul_test.cpp` — Coherence, novelty, temporal error drive, dreams
- `tests/phase6_mirror_test.cpp` — Handshake (ZKP), ontology mapping, fusion, schism

### /ontological_curiosity/
**Purpose:** Explores the boundaries of what the system can become
- `tests/phase8_integration_test.cpp` — Full-stack soak: language emergence, physics, KC
- `tests/phase11_endurance_test.cpp` — 120K-op self-surgery endurance under stress

### /genesis/
**Purpose:** The meta-optimizer that watches the watchers
- `tests/phase0_genesis_test.cpp` — Pacemaker oscillator, telemetry monitoring, phenotype adaptation

### /seed_body_mind/
**Purpose:** Foundational capabilities (seed, body, mind)
- `tests/phase1_seed_test.cpp` — Small-world network, GA, reservoir, pacemaker
- `tests/phase2_body_test.cpp` — Free energy, self-antigen, inflammation response
- `tests/phase3_mind_test.cpp` — Hivemind, agent genesis/death, reputation ledger

### /language_grounding/
**Purpose:** Language as tool for cognition
- `tests/phase5_language_test.cpp` — Grounding, neologism, semantic drive, Xeno-empathy

### /stress_resilience/
**Purpose:** Boundary condition enforcement (HELD vs BROKE)
- `tests/phase9_stress_test.cpp` — Breaking-point sweep (NaN, overflow, boundary, memory pool, sharded store)

## Constitutional Enforcement

The tests in each physiological function are **invariants**, not assertions. The Prime Directive is not a comment in the code. It's the test harness itself.

When a physiological function test fails:
1. The Governor detects the failure via Constitution::gate_parallel()
2. The Surgeon General initiates a recovery arc
3. The Pacemaker adjusts phenotype parameters to restore health
4. The system either recovers or enters a controlled shutdown

This is **immune system behavior, encoded at the test level.**

## Integration with Build System

The actual test files remain in `tests/` for build system compatibility. This directory provides the **constitutional mapping** that elevates tests from verification to legislation.

To run all physiological function tests:
```bash
make test
```

To run a specific physiological function:
```bash
make test0  # Genesis
make test2  # Body (coherence_preservation)
make test7  # Challenge (novelty_harvesting)
make test10 # Constitution (coherence_preservation)
make test12 # Dream (novelty_harvesting)
```
